interface Session {
  "code" string,  // Isso aq pode ser gerado/atualizado toda vez que startar..
  "name": string,
  "code": string,
  "rounds": int, # Atributo derivado no banco de dados
  "current_round": int,
  "player_count": int,
  "player_limit": int,
  "time_limit": int, #Tempo em segundos pra converter depois
  "stages": Stage[],
  "status": "inactive" | "lobby" | "playing" | "end",
}

interface Location{
  "image_url": string, 
  "longitude": float,
  "latitude" float,
  "place_name": string,
  "category": string,
}

Provavelmente precisa de uma tabela intermediaria pra conectar os dois (round
e location + session)

interface Round{
  "session": Session,
  "location": Location,
  "round_number": int,
}

interface Player { # Não sei se vai precisar por no banco de dados, posso
                          # Fazer tudo direto no front_end.
  "id": uuid;
  "session_id: uuid;
  "nickname": string;
  "is_conected": bool;
  avatar_config: {
    face: string; // ID ou URL do asset
    hat: string;
    accessory: string;
    color: string;
  };
  "score": float;
  "last_round_score": float;
}


interface Guess {
  player_id: uuid;
  round_id: uuid;
  latitude_guess: float;  // Onde ele clicou
  longitude_guess: float;
  distance_in_meters: float; // Calculado pelo backend
  points_awarded: int;     // Calculado pelo backend
  timestamp: datetime;     // Pra desempate (quem respondeu mais rápido)
}


